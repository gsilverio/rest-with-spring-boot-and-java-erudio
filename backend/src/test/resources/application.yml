server:
    port: 8888

spring:
  jpa:
    hibernate:
      ddl-auto: none
    #properties:
      #hibernate:
       # dialect: org.hibernate.dialect.PostgreSQLDialect
      show_sql: false
    open-in-view: false
springdoc:
  paths-to-match: /api/**/v1/**
  swagger-ui:
    use-root-path: true